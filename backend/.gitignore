# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
wheels/

# Virtual environment
.venv/
venv/
env/

# Environment variables & secrets
.env
.env.*
!.env.example
config.local.py
config.py

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/

# JSON data files
*.json
# ...except the seed catalogue, which is the one JSON file meant to ship: it is
# what gives a fresh clone working local search. Every field in it is Yahoo's,
# so it carries no broker identifiers (see README, "The seed file").
!seed/catalogue.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
test.py
# The pre-Postgres stocky.db, kept until the import is trusted (see README)
data/
