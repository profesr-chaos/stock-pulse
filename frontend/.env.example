# Copy to .env. The backend's default bind address. Also read by index.html's
# hero-data preload, which is why it is defined rather than left to the code
# fallback: an undefined %VITE_API_URL% makes the build warn on every run.
VITE_API_URL=http://127.0.0.1:5000
